{
  "name": "universal-clipboard",
  "version": "1.0.0",
  "private": true,
  "description": "Self-hosted local clipboard sync — no cloud required",
  "scripts": {
    "predev": "node scripts/setup.mjs",
    "prestart": "node scripts/setup.mjs",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "start": "npm run build --prefix frontend && npm run start --prefix backend",
    "server": "npm run start --prefix backend",
    "client": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
